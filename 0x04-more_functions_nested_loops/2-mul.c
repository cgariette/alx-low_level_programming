#include "main.h"
/**
 *  mul - multiplies two integers
 * @a: The first integer to be checked.
 * @b: The scond integer to be checked.
 * Return: mul after the multiplication of the teo integers.
 */
int mul(int a, int b)
{
int mult = a * b;
return (mult);
}
